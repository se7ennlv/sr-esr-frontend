import { prop, required } from '@rxweb/reactive-form-validators';

export class SroModel {
    @prop({ defaultValue: '' })
    umid: string;

    @required()
    fullName: string;

    @prop({ defaultValue: 0 })
    q111: number;

    @prop({ defaultValue: 0 })
    q112: number;

    @prop({ defaultValue: 0 })
    q113: number;

    @prop({ defaultValue: 0 })
    q121: number;

    @prop({ defaultValue: 0 })
    q122: number;

    @prop({ defaultValue: 0 })
    q123: number;

    @prop({ defaultValue: '' })
    q13: string;

    @prop({ defaultValue: '' })
    q141: number;

    @prop({ defaultValue: '' })
    q142: number;

    @prop({ defaultValue: 0 })
    q151: number;

    @prop({ defaultValue: 0 })
    q152: number;

    @prop({ defaultValue: 0 })
    q153: number;

    @prop({ defaultValue: 0 })
    q161: number;

    @prop({ defaultValue: 0 })
    q162: number;

    @prop({ defaultValue: 0 })
    q163: number;

    @prop({ defaultValue: 0 })
    q171: number;

    @prop({ defaultValue: 0 })
    q211: number;

    @prop({ defaultValue: 0 })
    q212: number;

    @prop({ defaultValue: 0 })
    q213: number;

    @prop({ defaultValue: 0 })
    q221: number;

    @prop({ defaultValue: 0 })
    q222: number;

    @prop({ defaultValue: 0 })
    q223: number;

    @prop({ defaultValue: 0 })
    q231: number;

    @prop({ defaultValue: 0 })
    q232: number;

    @prop({ defaultValue: 0 })
    q233: number;

    @prop({ defaultValue: 0 })
    q241: number;

    @prop({ defaultValue: 0 })
    q242: number;

    @prop({ defaultValue: 0 })
    q243: number;

    @prop({ defaultValue: 0 })
    q251: number;

    @prop({ defaultValue: 0 })
    q252: number;

    @prop({ defaultValue: 0 })
    q253: number;

    @prop({ defaultValue: 0 })
    q261: number;

    @prop({ defaultValue: 0 })
    q262: number;

    @prop({ defaultValue: 0 })
    q263: number;

    @prop({ defaultValue: 0 })
    q271: number;

    @prop({ defaultValue: 0 })
    q272: number;

    @prop({ defaultValue: 0 })
    q273: number;

    @prop({ defaultValue: 0 })
    q281: number;

    @prop({ defaultValue: 0 })
    q282: number;

    @prop({ defaultValue: 0 })
    q283: number;

    @prop({ defaultValue: 0 })
    q284: number;

    @prop({ defaultValue: 0 })
    q285: number;

    @prop({ defaultValue: 0 })
    q286: number;

    @prop({ defaultValue: 0 })
    q287: number;

    @prop({ defaultValue: 0 })
    q288: number;

    @prop({ defaultValue: '' })
    q29: string;

    @prop({ defaultValue: '' })
    q31: string;

    @prop({ defaultValue: '' })
    q32: string;

    @prop({ defaultValue: '' })
    q33: string;

    @prop({ defaultValue: 0 })
    q341: number;

    @prop({ defaultValue: 0 })
    q351: number;

    @prop({ defaultValue: 0 })
    q352: number;

    @prop({ defaultValue: 0 })
    q353: number;

    @prop({ defaultValue: 0 })
    q354: number;

    @prop({ defaultValue: '' })
    q41: string;

    @prop({ defaultValue: 0 })
    q42: number;

    @prop({ defaultValue: '' })
    q43: string;

    @prop({ defaultValue: '' })
    q44: string;

    @prop({ defaultValue: '' })
    q45: string;

    @prop({ defaultValue: 0 })
    q51: number;

    @prop({ defaultValue: 0 })
    q52: number;

    @prop({ defaultValue: 0 })
    q53: number;

    @prop({ defaultValue: 0 })
    q54: number;

    @prop({ defaultValue: 0 })
    q55: number;

    @prop({ defaultValue: '' })
    q56: number;

    @required()
    surveyAt: Date;

    @prop({ defaultValue: '' })
    comment: string;
}